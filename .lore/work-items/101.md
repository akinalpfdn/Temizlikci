---
{
  "codeAnchors" : [

  ],
  "createdAt" : "2026-09-21T22:43:38Z",
  "createdBy" : "claude",
  "formatVersion" : 1,
  "id" : "101",
  "priority" : "high",
  "relatedWorkItemIDs" : [

  ],
  "status" : "inbox",
  "title" : "Startup-disk scan peaks at ~1.1 GB resident memory",
  "type" : "knownIssue",
  "updatedAt" : "2026-09-22T02:08:27Z"
}
---

Measured 2026-09-22 in the running Debug app (ps RSS every second) during a full "Macintosh HD" scan: ~2 min at ~378% CPU, peak RSS 1,112 MB at the end of the scan. NOTE: that scan still counted the disk twice through /.nofollow (fixed in 0d88405), so the real figure was roughly half.

Progress (5c96931 "perf(scan): derive node id and name from the URL instead of storing them"):
- Retained heap of a ~/Documents/GitHub result (93,565 folders): 107 MB → 69 MB (−36%), ~1,204 → ~780 bytes per folder, stable across two runs each. Peak RSS 166–216 → 117–126 MB (noisy).
- The benchmark now reports retained heap (malloc_zone_statistics), which is stable, instead of relying on peak RSS.

Remaining: measure the startup disk in the running app with the developer (target < ~500 MB). Next lever if needed: store only names and derive URLs from the ancestor chain.